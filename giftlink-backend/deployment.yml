apiVersion: apps/v1
kind: Deployment
metadata:
  name: giftapp
  labels:
    app: giftapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: giftapp
  template:
    metadata:
      labels:
        app: giftapp
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault

      imagePullSecrets:
        - name: icr

      containers:
        - name: giftapp-container
          image: us.icr.io/sn-labs-prathamchand/giftapp:latest
          imagePullPolicy: Always

          ports:
            - containerPort: 3060

          env:
            - name: MONGO_URI
              value: "mongodb://172.21.36.205:27017"

            - name: DB_NAME
              value: "giftsdb"

            - name: PORT
              value: "3060"

            - name: JWT_SECRET
              value: "your_secret_here"

          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL

---
apiVersion: v1
kind: Service
metadata:
  name: gift-app-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 3060
      protocol: TCP
  selector:
    app: giftapp